<h1>Editing Book</h1>
<!--編集画面-->
<%= form_with model: @book do |f| %>

  <h4>Tilte</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>
  <% end %>

<!--showの追加　移動先がindexになっているので修正が必要-->
<%= link_to "show" , books_path(@book) %>
|
<!--backの追加-->
<%= link_to "Back" , books_path %>